// Patty Code account service — email/password auth, sessions, and public profiles
// for patty-code.io. The Hono app is itself the Workers fetch handler.
import app from "./app";

export default app;
